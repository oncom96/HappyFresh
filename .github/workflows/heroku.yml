name: Push Container to Heroku

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Login to Heroku Container registry
      env: 
        HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      run: heroku container:login 
    - name: Build and push
      env:
        HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      run: heroku container:push -a ${{ secrets.HEROKU_APP_NAME }} Happy
    - name: Release
      env:
        HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
<<<<<<< HEAD
      run: heroku container:release -a ${{ secrets.HEROKU_APP_NAME }} Happy
=======
      run: heroku container:release -a ${{ secrets.HEROKU_APP_NAME }} Happy
>>>>>>> 103921924c8824d49b8d47ec38d0e2848733e1aa
